var once = function(fn) {
    let called = false;   // track if function is called
    let result;

    return function(...args){
        if (!called) {
            called = true;
            result = fn(...args);
            return result;
        }
        return undefined;
    }
};
